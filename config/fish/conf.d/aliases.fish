alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"

alias b="bundle"
alias be="bundle exec"

alias migrate="rake db:migrate db:rollback && rake db:migrate db:test:prepare"
alias s="rspec"

alias vimrc="$VISUAL $HOME/.vim/vimrc"
